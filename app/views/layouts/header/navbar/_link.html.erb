<%= link_to title,
            url,
            class: "hover-underline px-1 text-lg font-display hover:text-primary hover:transition active:text-primary-press
                    #{ active ? 'font-bold text-primary' : 'font-light' }" %>